	import { all } from ./chronos.src/node.indexUV.js
	import { construct } from ./chronos.src/node.translateWebpack.js
		import { bundles } from ./chronos.src/node.bundler.json // import bundled scripts
		import { webpackManifest } from ./chronos.src/node.bundler.json // import bundled scripts
		import { webpackManifestHost } from ./chronos.src/uv/uv.manifest.json // import bundled scripts
	// !important clause /\/
	import { alertErr } from ./chronos.src/err.js
	// import default
			const Constructor = function(whatMethod, whatElement, formID) {
		let target = this.href;
			let searchForm = this.linkName;
			if (whatMethod = "useTranslate") 
				// var let": form user
				let uM = document.getElementByID(formID);
				let uX = document.getElementByID(formID).value;
				let u3 = document.getElementByID(whatElement).value;
			// construct function from node.translateWebpack.js
				construct(u3)
			if (ERR_STATEMENT = "errorInExecution") {
				alertErr("alertBox","An error has occured. Please check the URL and try again.");
			} else if () {}; } //null 
			} else if whatMethod = "useUV"{
				
			}
		}
		function startNode () {
			
		}